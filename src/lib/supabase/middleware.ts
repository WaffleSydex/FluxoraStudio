// Intentionally unused.
//
// Admin auth uses an env-var username/password with an HMAC-signed cookie
// (see src/lib/admin-auth.ts and src/middleware.ts), not Supabase Auth.
// This file is kept as a placeholder to avoid stale imports.

export {};
